import { DrizzleChat } from '@/lib/db/schema'
import React from 'react'

type Props = {
    chat: DrizzleChat;
}

const ChatCard = ({chat}: Props) => {
  return (
    <div className="max-w-xl border-1 border-gray-700">

    </div>
  )
}

export default ChatCard